# Add project specific ProGuard rules here.
# minifyEnabled is off by default for this project (see app/build.gradle.kts).
# Keep Room entities/converters if minification is turned on later.
-keep class com.fridgetracker.app.data.** { *; }
